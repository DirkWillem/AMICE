#ifndef AMICE_UTILS_H
#define AMICE_UTILS_H

#include <cstdint>

namespace core {

/**
 * Waits for the given amount of milliseconds
 * @param ms Number of milliseconds to wait
 */
void WaitMs(uint32_t ms);

}

#endif //AMICE_UTILS_H
